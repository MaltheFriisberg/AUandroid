package app.athleteunbound.RESTapiUtils.Helpers;

import android.content.SharedPreferences;
import android.preference.PreferenceManager;

/**
 * Created by Mal on 14-05-2016.
 */
public class TokenHandler {

    /*public String getCurrentToken() {
        SharedPreferences settings = PreferenceManager
                .getDefaultSharedPreferences(this);

        return settings.getString("AthleteUnboundApiToken", "");
    }
    public void saveNewToken(String token) {
        SharedPreferences settings = PreferenceManager
                .getDefaultSharedPreferences(this);

        SharedPreferences.Editor editor = settings.edit();
        editor.putString("AthleteUnboundApiToken", token);
        editor.commit();
    }*/
}
